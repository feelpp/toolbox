= Electrostatics toolbox
:page-tags: manual
:description: Electrostatics toolbox modeling theory
:page-illustration: pass:[toolboxes::manual.svg]

This toolbox solves a quasi-static conduction model for the electric potential stem:[V] in conducting materials.
The starting point is the xref:toolboxes:maxwell:Maxwell.adoc[Maxwell equations].

== Fields and parameters (names and units)

=== Fields

[options="header"]
|===
| Symbol | Physical name | Toolbox field name | SI unit | Role

| stem:[V]
| electric potential
| `electric-potential`
| stem:[V]
| primary unknown

| stem:[\mathbf{E}]
| electric field
| `electric-field`
| stem:[V.m^{-1}]
| derived from stem:[-\nabla V]

| stem:[\mathbf{J}]
| current density
| `current-density`
| stem:[A.m^{-2}]
| derived from stem:[-\sigma \nabla V]

| stem:[q_J]
| Joule losses density
| `joules-losses`
| stem:[W.m^{-3}]
| derived from stem:[\sigma |\nabla V|^2]

| stem:[\mathbf{D}]
| electric displacement
| not exported
| stem:[C.m^{-2}]
| used in transient formulation

| stem:[\rho]
| electric charge density
| not exported
| stem:[C.m^{-3}]
| used in transient formulation
|===

=== Required material properties and units

[options="header"]
|===
| Property | Symbol | SI unit | Required in steady model | Required in transient model | Possible dependency

| electric conductivity
| stem:[\sigma]
| stem:[S.m^{-1}]
| yes
| yes
| may depend on stem:[\mathbf{E}] (e.g. stem:[\sigma=\sigma(\mathbf{E})]), and more generally on model symbols

| electric permittivity
| stem:[\epsilon]
| stem:[F.m^{-1}]
| no
| yes
| may depend on stem:[\mathbf{E}] (e.g. stem:[\epsilon=\epsilon(\mathbf{E})]) for nonlinear dielectrics
|===

NOTE: In the current toolbox implementation, stem:[\sigma] is available as material property while stem:[\epsilon] is not yet assembled in the electric solver.

=== Parameters and boundary data

[options="header"]
|===
| Symbol | Name | JSON name/key | SI unit | Comment

| stem:[\sigma]
| electric conductivity
| material property `sigma` (`electric-conductivity`)
| stem:[S.m^{-1}]
| required in current implementation, can be nonlinear (e.g. stem:[\sigma(\mathbf{E})])

| stem:[\epsilon]
| electric permittivity
| planned transient material property
| stem:[F.m^{-1}]
| transient target model property, can be nonlinear (e.g. stem:[\epsilon(\mathbf{E})]); not yet assembled in current implementation

| stem:[V_D]
| imposed potential
| BC `electric_potential` / `electric_potential_imposed`
| stem:[V]
| Dirichlet boundary data

| stem:[g]
| imposed normal current density
| BC `surface_charge_density` / `charge_density`
| stem:[A.m^{-2}]
| Neumann-type boundary data

| stem:[I]
| imposed total current
| integral-current condition
| stem:[A]
| currently unavailable in this toolbox

| stem:[t]
| time
| model parameter `t`
| stem:[s]
| used in transient expressions

| stem:[\mathbf{n}]
| outward unit normal
| geometric quantity
| dimensionless
| boundary orientation
|===

== Quasi-static electric model

In quasi-static conditions, the charge conservation equation is

[stem]
++++
\nabla \cdot \mathbf{J} = 0
++++

with Ohm's law

[stem]
++++
\mathbf{J} = -\sigma \nabla V
++++

which gives the strong form solved in this toolbox:

[stem]
++++
\nabla \cdot \left( \sigma \nabla V \right) = 0 \quad \text{in } \Omega.
++++

The unknown is the scalar potential stem:[V]. The electric field and current density are then recovered from stem:[V].
From a physical viewpoint, this is a conduction model: currents are driven by potential differences and weighted locally by the conductivity.

For multiple materials stem:[\Omega_i], the conductivity stem:[\sigma_i] is piecewise defined.
The implementation enforces continuity through the conforming finite-element space across interfaces.

NOTE: A volumetric electric source term (Poisson right-hand side) is currently not assembled in this toolbox implementation.

== Derived quantities

After solving for stem:[V], the toolbox computes

[stem]
++++
\mathbf{E} = -\nabla V, \qquad
\mathbf{J} = -\sigma \nabla V, \qquad
q_J = \sigma |\nabla V|^2.
++++

where stem:[q_J] is the Joule losses density.

== Boundary conditions

Boundary conditions encode how the device is electrically connected to its environment.
In practice, one usually combines potential constraints (to drive current and fix the reference level) with natural/flux conditions on other boundaries.

=== Electric potential imposed (Dirichlet)

On stem:[\Gamma_D]:

[stem]
++++
V = V_D.
++++

This is exposed in JSON with `electric_potential` (or alias `electric_potential_imposed`).
This boundary condition is typically used on terminals where voltage is controlled.

=== Ground

Ground is a specialized Dirichlet condition:

[stem]
++++
V = 0 \quad \text{on } \Gamma_g.
++++

This is exposed with the JSON key `ground`.
It provides a potential reference and avoids the constant-offset indeterminacy of pure Neumann problems.

=== Surface charge density / normal current density (Neumann)

On stem:[\Gamma_N]:

[stem]
++++
\mathbf{J} \cdot \mathbf{n} = g
\quad \Leftrightarrow \quad
-\sigma \nabla V \cdot \mathbf{n} = g.
++++

This is exposed with `surface_charge_density` (alias `charge_density`).
It models injected/extracted normal current density on a boundary.
If no such condition is set on a boundary part, the natural homogeneous condition applies:

[stem]
++++
\mathbf{J}\cdot\mathbf{n}=0.
++++

=== Integral current condition

The integral-current boundary condition

[stem]
++++
\int_{\Gamma_I}\mathbf{J}\cdot\mathbf{n}\,d\Gamma = I
++++

is not available in this standard electric toolbox implementation (it is an HDG-oriented feature).
Conceptually, this condition is useful for terminal-driven models where total current is prescribed instead of pointwise flux.

== Time dependence

For time-dependent electric evolution in conducting media, a transient electro-quasistatic model can be considered.
Compared with the steady model, it introduces capacitive effects through the displacement field.

Starting from charge conservation:

[stem]
++++
\partial_t \rho + \nabla\cdot\mathbf{J}=0
++++

with

[stem]
++++
\mathbf{E}=-\nabla V,\qquad
\mathbf{D}=\epsilon\mathbf{E}=-\epsilon\nabla V,\qquad
\rho=\nabla\cdot\mathbf{D},
++++

and conductive current

[stem]
++++
\mathbf{J}=-\sigma\nabla V,
++++

the transient electro-quasistatic PDE reads

[stem]
++++
\nabla\cdot\left(\sigma\nabla V + \epsilon\,\partial_t\nabla V\right)=0.
++++

When stem:[\epsilon\to 0], this model reduces to the steady conduction equation.

=== Initial condition (transient case)

A transient solve requires an initial state, for example:

[stem]
++++
V(\mathbf{x},0)=V_0(\mathbf{x}).
++++

=== Boundary-condition interpretation in transient mode

Potential (Dirichlet) conditions keep the same form as in steady mode.
For Neumann-like boundaries, one must choose whether the imposed normal quantity is:

- conductive current only: stem:[\mathbf{J}\cdot\mathbf{n}]
- or total current including displacement: stem:[(\mathbf{J}+\partial_t\mathbf{D})\cdot\mathbf{n}]

This choice must be fixed consistently in model equations, weak form, and user-facing boundary-condition semantics.

NOTE: The current electric toolbox implementation remains steady-state only. The transient formulation above is a target model for future implementation.
